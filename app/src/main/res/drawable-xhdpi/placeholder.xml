<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android"
    android:opacity="opaque">
    <!-- The background color, preferably the same as your normal theme -->
    <item android:drawable="@color/colorPrimary" />
    <!-- The product logo with top margin and centered horizontally -->
    <item
        android:bottom="@dimen/dimen_30"
        android:drawable="@drawable/ic_gallery"
        android:left="@dimen/dimen_30"
        android:right="@dimen/dimen_30"
        android:top="@dimen/dimen_30" />
</layer-list>